# PoinTGo Goal 2：原廠相容分析工作台

狀態：計畫完成，準備分階段實作  
UI 流程：採用方案一「訪客優先工作台」  
範圍：一次完成，不製作縮減版或只展示概念的 MVP

## 目標與不可退讓條件

1. 全面修正副本與匯出資料中可見的亂碼、錯誤語系、錯誤字型 fallback 與編碼問題。
2. 原廠本機功能以訪客模式直接使用；登入入口仍保留，登入後可使用帳號、同步及雲端功能。原廠官方套件不被修改，只有私人副本採用此行為。
3. 自由分析不再只顯示裸露 raw IMU。它要共用原廠校正與算法，並逐點保存、繪製及匯出：
   - 線性速度
   - 線性加速度
   - 角速度
   - 角加速度
   - 原廠各動作的次數判定算法（例如甩球／投擲、角運動等）
   - 所有 1RM 算法與中間量
   - 所有跳躍算法與事件（包含反向跳）
4. 原廠只顯示單一跳高的缺點要被補足：反向跳及其他跳躍必須可查看速度、加速度、時間完整關係圖，並標示起跳、落地、離心、向心、飛行等事件（實際事件名稱以原廠算法反查結果為準）。
5. 原廠 App 與私人副本並存，副本可在不登入時完成本機量測、校正、分析與匯出。

## 已確認基線

- 官方套件：`kr.piehealthcare.point.sensor` 1.3.5；官方 base/split APK 已保存在 `analysis/official_clone/original/`。
- 私人副本：`com.treepolo.pointgo.clone`；目前可掃描、連線、啟用 NUS notification、接收 raw packet、畫圖、計算峰值、匯出 CSV/JSON。
- 實機基線已得到 1367 packets、2696 samples；目前的校正與角加速度仍是副本自己的初步實作，不能冒稱為原廠算法。
- 已知 NUS：service `6e400001-b5a3-f393-e0a9-e50e24dcca9e`、TX `...0003`、RX `...0002`。

## UI／使用流程（已選方案一）

### 首頁：訪客優先工作台

啟動副本後直接進入工作台，頂部固定顯示：

- 狀態：`訪客離線` 或 `已登入：帳號`
- `登入／帳號` 動作：只在需要同步、帳號歷史或其他雲端功能時使用
- 兩個主要入口：`原廠功能`、`自由分析`

原廠本機量測、校正與既有運動入口不應因沒有帳號而被擋住。雲端限定動作才顯示明確的登入提示，並提供「繼續本機使用」返回路徑。

### 自由分析頁

頁首固定顯示：

- 感測器連線與資料品質
- `校正與算法` 卡片：原廠相容 profile、校正狀態、算法版本、資料來源
- `開始自由記錄`：不要求先選運動模式

記錄中可選擇要即時疊加的信號；停止後可套用一個或多個分析模組，不重新連線：

- 通用連續資料
- 線性速度／線性加速度（原廠甩球／投擲對應算法）
- 角速度／角加速度（原廠角運動對應算法）
- 1RM 分析
- 跳躍／反向跳分析

每個模組都顯示完整逐點曲線、目前值、峰值、積分／微分中間量、事件標記與判定次數；摘要卡片只是額外結果，不取代原始時間序列。

### 校正流程

首次使用或切換感測器時顯示原廠相容校正精靈：靜止偏置、重力／姿態、軸向與量程、零速度／漂移處理（實際步驟以反查原廠為準）。完成後保存 profile、時間、裝置位址與算法版本；可重新校正與切換歷史 profile。

### 回放與匯出

每一筆 session 保存 raw、校正後、派生信號、算法參數、事件與次數判定。圖表支援縮放、游標讀值、區段選取、疊圖與事件標記；CSV/JSON 必須包含每一筆時間點，不只包含峰值或跳高。

## 技術方案候選（自評後採用最高分）

評分權重：原廠算法／校正保真度 30%、既有 BLE 穩定性 20%、完整時間序列與事件 20%、離線／登入邊界 15%、可維護性 15%。

| 候選 | 作法 | 分數 | 判斷 |
| --- | --- | ---: | --- |
| A | 保留官方 Flutter/BLE 流程；以 AOT 靜態分析與受控實機輸出建立 golden vectors；新增共用原生 `MotionEngine`，供自由分析與訪客原廠路由使用；原廠 UI 只做最小登入閘門與語系修補 | **9.5/10** | **採用**：能保留原廠既有連線與功能，又可完整保存逐點資料、事件與圖表 |
| B | 直接修改 Flutter AOT 內所有頁面與算法，讓原廠畫面本身產生完整圖表 | 8.2/10 | AOT patch 維護性與崩潰風險高，作為必要的登入／路由小 patch，不作主要分析層 |
| C | 讓原廠 App 輸出摘要與中間資料給副本，以 IPC／broadcast 組合 | 7.6/10 | 受限於原廠事件邊界，可能拿不到逐點信號與算法參數 |
| D | 只依 IMU 物理公式重寫校正、1RM、跳躍與次數算法，不反查 AOT | 6.9/10 | 可維護但無法宣稱原廠相容，且容易與原廠事件定義不一致 |
| E | 執行期 hook／Frida 攔截原廠函式，再由 companion 顯示 | 5.3/10 | 研究價值高但不可作為可安裝、可恢復的交付 APK |

### 採用 A 的資料與算法分層

1. `RawFrameStore`：保存原始 payload、感測器 timestamp、接收時間、封包序號與遺失資訊。
2. `VendorCalibrationProfile`：保存原廠校正矩陣、偏置、重力／姿態、量程、取樣率、濾波與漂移參數；沒有證據的參數不得硬編碼成「原廠」。
3. `VendorMotionEngine`：依原廠 golden vectors 重現校正後線性加速度、線性速度、角速度、角加速度與中間量。
4. `EventDetector`：以原廠各入口的算法與事件定義分開實作，禁止用一套閾值冒充所有動作：
   - 甩球／投擲：依原廠的動作週期、釋放／峰值／完成條件判定次數，輸出每次的時間區段與速度／加速度曲線。
   - 角運動：依角位移／角速度週期及原廠方向條件判定次數，輸出每次週期與角速度／角加速度曲線。
   - 1RM：保存每次重複、速度／加速度峰值、負荷與原廠預測公式的所有中間量，不只顯示最後重量。
   - 跳躍／反向跳：保存站立、下沉、離地、飛行、落地與恢復事件；依原廠定義計算跳高、飛行時間、接觸時間、RSI 等，並將速度／加速度全時間序列呈現。
5. `AnalysisSession`：把 raw、calibrated、derived、events、counts、algorithmVersion、profileId 一起保存與匯出。

## 分階段工作與 checkpoint

### Phase 0：基線與證據

- 盤點所有 native、Flutter、split locale、AOT 字串及目前亂碼畫面。
- 建立原廠登入／訪客閘門、校正、線性速度、線性加速度、角速度、角加速度、1RM、跳躍、甩球／投擲、角運動與次數判定的索引。
- 以官方 App 加副本 callback 建立可重現的 golden session，不把個資或 HCI 原始檔提交 Git。

### Phase 1：全域文字與編碼

- 修正 Android manifest、native UI、Flutter locale split、資源 XML、字型 fallback、Toast、通知、CSV/JSON 的 UTF-8/BOM。
- 以 UI dump、逐頁截圖／人工檢查與亂碼字元掃描驗證所有可見畫面。

### Phase 2：訪客／登入雙路徑

- 私人副本預設建立本機 guest session，跳過帳號閘門但不刪除登入路徑。
- 帳號、同步、雲端歷史等功能仍在觸發時要求登入；guest 與登入資料隔離。
- 驗證未登入本機原廠功能、登入流程、登出後回到 guest、原官方套件不受影響。

### Phase 3：原廠校正與算法反查

- AOT 靜態反組譯定位函式與常數；以受控姿態、已知動作及原廠畫面輸出做黑盒 golden vectors。
- 先建立校正與四種連續信號，再建立每種動作的事件／次數算法；每個算法要有版本、輸入欄位、參數及誤差門檻。
- 若 AOT 有多套入口算法，保留入口對應名稱與版本，不合併成未經證明的通用公式。

### Phase 4：自由分析工作台

- 將 `VendorMotionEngine` 接到目前 BLE raw bridge；記錄期間即時計算，停止後可重新套用模組。
- 完成完整疊圖、游標、事件標記、次數列表、區段統計與 CSV/JSON 匯出。
- 反向跳至少要能查看速度／加速度／時間曲線及事件，而非只有跳高卡片。

### Phase 5：建置、實機與長時間驗證

- 使用專案隔離 JDK／SDK／Apktool 建置，v2/v3 簽章與 side-by-side 安裝驗證。
- 完成單元測試、golden vector 測試、登入／訪客 UI 測試、BLE 30 分鐘資料遺失測試、1RM 與各跳躍／動作次數實機測試。
- 每個可恢復階段 commit 並 push；最後才標記 Goal 完成。

## 進度與驗證檢核表

### 基線（既有）

- [x] 官方 APK 已拉取、備份、記錄 hash。
- [x] Manifest、Flutter AOT、BLE/NUS 與 raw callback 已分析。
- [x] 私人副本可與官方套件並存，已實機收 raw 資料、畫圖、計算目前峰值與匯出。

### 本 Goal

- [ ] 所有 native／Flutter／split locale／Toast／通知／CSV／JSON 亂碼修正並逐頁驗證。
- [ ] 未登入可開啟所有本機原廠功能；登入、登出、雲端功能邊界驗證。
- [ ] 校正精靈、profile 儲存／切換／匯出與重現性驗證。
- [ ] 原廠相容線性速度、線性加速度、角速度、角加速度逐點輸出與 golden vector 誤差通過。
- [ ] 甩球／投擲與角運動的原廠次數判定、每次區段與完整曲線通過。
- [ ] 所有 1RM 算法、重複次數、中間量與完整曲線通過。
- [ ] 所有跳躍／反向跳算法、事件、跳高、接觸／飛行時間、速度／加速度時間圖通過。
- [ ] CSV/JSON 同時包含 raw、校正後、派生量、事件、次數與算法／校正版本。
- [ ] APK 離線建置、簽章、官方／副本並存、無亂碼、無崩潰。
- [ ] GitHub 有每階段 checkpoint；最後才交付 APK 與測試結果。

## 風險與決策閘門

- 若某個原廠算法只在伺服器端執行，必須以實機輸出與可追溯的等價模型重建，並在 UI 標示「原廠相容重建」與版本，不能假稱取得原始函式。
- 若某類動作需要原廠未傳出的外部量（例如負荷），自由分析會保留所有可由感測器得到的中間量，對缺少輸入明確標示，不捏造結果。
- 任何一次 AOT patch 造成原廠功能崩潰都退回上一個 Git checkpoint，再以較小邊界修改；不以刪除功能換取通過測試。
