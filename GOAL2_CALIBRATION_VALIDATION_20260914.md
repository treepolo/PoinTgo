# Goal 2 校正品質修正與驗證

更新：2026-09-14

## 本次修正

- 六面校正的軸向比例改為保留正負號。舊寫法對正反面差值取絕對值，會讓感測器以反向安裝或軸向定義相反時，校正後的全域方向反轉。
- 六面校正完成前加入品質閘門：每一面都必須在預期軸上看到足夠重力、主軸方向明確，且取樣殘差不能顯示明顯晃動。
- 校正精靈會在取樣狀態顯示品質提示；若六面資料品質不足，按完成時不會覆寫既有 profile，並以繁體中文說明需要重取的面。

目前門檻為：預期軸重力至少 `0.45 g`、主軸相對橫向分量至少 `1.4x`、取樣殘差 RMS 不超過 `0.15 g`。這些是防止誤存校正資料的工程門檻，不宣稱與官方 AOT 逐位元相同。

## 可重現驗證

`powershell.exe -NoProfile -ExecutionPolicy Bypass -File .\\official-clone\\test-vendor-engine.ps1`

結果：`VENDOR_ENGINE_TEST_OK`

測試包含：

1. 六面資料的反向軸向約定；校正後仍能還原預期的正向重力。
2. 六面全零資料；品質檢查失敗且 `finish()` 不會把不合格資料標成完成。
3. 既有通用、甩球／投擲、角運動、VBT、1RM、跳躍／CMJ 與 JSON 回歸向量。

## 實機狀態

新的 debug APK 已以隔離 JDK／Android SDK 建置、完成 v2/v3 簽章並安裝至 Samsung SM-N9810。實際六面固定姿態仍待使用者依校正精靈逐面放置感測器後驗證；在該步驟完成前，進度表不會把實機校正標成完成。
