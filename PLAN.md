# PoinT GO Motion Analyzer 開發計畫

## 目標

建立一個可直接連接 PoinT GO 感測器的 Android companion App，補足原廠 Poin*T Go App 的限制：

- 不必先選擇運動模式即可開始記錄。
- 即時顯示線性加速度與角加速度的時間序列。
- 顯示 X/Y/Z、合成量與可設定的峰值（線性與角加速度）。
- 記錄完成後可回放、縮放、標註、分段比較並匯出 CSV／JSON。
- 以即時門檻、視覺色彩與可選聲音提醒協助動作品質監測。

## 目前已知條件

- 目標裝置：Samsung SM-N9810（Android），ADB 已授權。
- 原廠套件：`kr.piehealthcare.point.sensor`，版本 1.3.5。
- 尚無原廠 App 原始碼，也尚未取得 PoinT GO BLE GATT／封包規格。
- 官方公開資料宣稱 9 軸 IMU，但資料取樣率、重量與續航等規格在不同頁面不一致；所有數值以實機封包為準。
- 因此預設做獨立 App，不直接重打包或修改原廠 APK；若協定需要原廠流程，才評估相容層。

## UI／使用流程（使用者已選 C+D+E）

### 即時監控儀表板

1. 開啟 App 後直接看到「開始記錄」，不要求先選運動模式。
2. 連線後顯示目前連線狀態、電量、取樣率與資料品質。
3. 即時雙圖：線性加速度與角加速度；可切換 X/Y/Z／合成量、原始／濾波。
4. 固定顯示目前值、峰值、RMS、門檻狀態；超過門檻以顏色和可選聲音提示。

### 快速／進階雙層

- 快速層：掃描、連線、開始、停止、儲存，四個主要動作即可完成。
- 進階層：軸向、單位、重力移除、濾波器、峰值視窗、門檻、取樣率與時間窗。

### 回放／標註

1. 記錄結束後自動進入回放頁，保留完整時間戳資料。
2. 可拖曳選取區段、加入動作名稱／備註、切換疊圖。
3. 每個區段顯示線性／角加速度峰值與時間位置，支援跨區段比較。
4. 可匯出 CSV（逐筆資料）與 JSON（量測設定、校正、標註、摘要）；圖表提供 PNG／SVG 匯出。

## 技術方案候選與評分

評分權重：BLE 可靠性 30%、高頻資料效能 25%、離線資料與匯出 20%、UI 可控性 15%、長期維護 10%。

| 方案 | 作法 | 評分 | 判斷 |
|---|---|---:|---|
| A | Kotlin 原生 + Jetpack Compose + BluetoothGatt／可替換協定層 + Room + Compose Canvas 圖表 | 9.4/10 | **採用**；最適合 Android BLE、高頻資料、離線回放與單一 App。 |
| B | Flutter + `flutter_blue_plus` + SQLite／Drift + Flutter 圖表 | 7.8/10 | 跨平台較快，但 BLE 背景與高頻通知排錯成本較高。 |
| C | React Native + `react-native-ble-plx` + JS 圖表 | 6.9/10 | UI 開發方便，但高頻資料、背景服務與原生相容性風險較高。 |
| D | 直接修改／重打包原廠 APK，重用其協定與資料流程 | 5.8/10 | 缺少原始碼與簽章，更新脆弱；只作協定研究備援，不作主線。 |
| E | 手機 App 只做顯示，透過 ADB／桌面程式收資料 | 4.2/10 | 不符合獨立使用與即時訓練需求，淘汰。 |

### 採用架構

- Android 原生 Kotlin，最低支援 Android 8／API 26；以現有手機 Android 版本驗證。
- Compose UI；資料圖表先用自繪 Canvas，避免高頻資料被圖表套件複製與 GC 影響。
- BLE 協定採 `SensorTransport` 介面，將掃描、連線、服務探索、訂閱通知、寫入控制與封包解碼分離。
- `RawSample` 儲存原始 timestamp、加速度、角速度、角加速度（若可由封包或差分取得）、校正與來源格式。
- `SessionRepository` 將量測資料先寫入本機資料庫／檔案，再以 ring buffer 驅動即時畫面；避免畫面刷新造成資料遺失。
- 匯出層固定輸出 SI 單位（m/s²、rad/s²），同時保留原始單位與轉換資訊。

## 平行工作流（技術方案 A 採用後）

1. **協定與實機偵察**：讀取原廠 APK 的 BLE UUID／封包線索，並在使用者正常連線一次時擷取 GATT 服務與通知形狀。
2. **Android 專案與資料層**：建立可建置的 Kotlin 專案、BLE 介面、資料模型、持久化與匯出骨架。
3. **UI／圖表／回放**：完成 C+D+E 流程、即時 ring buffer、峰值計算、標註與比較介面。

三條工作流合併前，所有外部資料都以測試資料與可追溯紀錄驗證；若協定偵察證實感測器只提供處理後峰值，會在技術閘門停下並明確標示無法從 App 端還原的項目。

## 進度與驗證檢核表

### 先決條件

- [ ] 感測器型號／外觀與 PoinT GO 實體確認。
- [ ] 使用原廠 App 正常連線一次，記錄裝置名稱、位址是否隨機化與連線時序。
- [ ] 取得至少一段 30 秒以上的實際 BLE 通知資料。
- [ ] 判定是否有原始加速度與角速度；若只有角速度，確認可否穩定差分為角加速度。

### 功能

- [ ] App 開啟後不選模式即可開始記錄。
- [ ] BLE 掃描、連線、重連、斷線提示與電量／取樣率顯示。
- [ ] 即時線性加速度圖（X/Y/Z／合成量）。
- [ ] 即時角加速度圖（X/Y/Z／合成量）。
- [ ] 峰值、RMS、峰值時間位置與門檻提示。
- [ ] 原始／濾波、單位、重力移除與時間窗設定。
- [ ] 記錄停止、完整回放、縮放、游標讀值。
- [ ] 標註與分段比較。
- [ ] CSV／JSON／PNG／SVG 匯出，且匯出內容與畫面摘要一致。
- [ ] App 重啟後可列出並開啟歷史量測。

### 品質與裝置驗證

- [ ] 連續記錄 10 分鐘無崩潰、無明顯遺失或時間戳倒退。
- [ ] 通知頻率與封包計數符合實測，解析後單位與軸向經靜止／已知動作校正。
- [ ] 峰值以固定視窗與測試向量重算，誤差在明確容許範圍內。
- [ ] 旋轉與重力測試能區分線性加速度、角速度與角加速度，並在 UI 標示資料來源。
- [ ] Samsung SM-N9810 實機安裝、連線、記錄、回放、匯出完整驗證。
- [ ] 建立 Git checkpoint；每個可恢復階段有測試紀錄。

## 風險與決策閘門

1. **沒有原始資料**：若 BLE 只傳峰值或加密資料，無法只靠 App 端精確還原完整時間序列；需改走官方 SDK、韌體支援或外加 IMU。
2. **角加速度定義**：若感測器只傳角速度，角加速度將由校正後的時間差分計算，並標示濾波與微分參數；不可把角速度誤標成角加速度。
3. **座標與重力**：必須記錄安裝方向與校正狀態，否則跨動作比較可能失真。
4. **取樣率／藍牙吞吐量**：先以實測通知頻率與封包大小決定 ring buffer、批次寫入與圖表刷新頻率。

